# Requirements

Status: Draft
Traces to: `intent.md` (not yet baselined)

Add stable `REQ-` identifiers and observable acceptance outcomes before writing application code. Every MVP must-have must map to at least one evaluation scenario.
