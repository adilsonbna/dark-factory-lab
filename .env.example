# Copy to .env locally. Never commit .env.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.8-flash
GITHUB_TOKEN=
# Required for state-changing operator API actions. Generate a strong local value.
OPERATOR_TOKEN=
REPOSITORY_REVISION=local
